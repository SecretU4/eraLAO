@SYSTEM_TITLE
#DIMS TITLE_HTML
;@SYSTEM_TITLE가 정의되고 있으면, 표준의 타이틀 화면 대신에 SYSTEM_TITLE가 불린다.

;이 타이밍에 글로벌 변수를 읽어 두면 잡기 엎지름이 없어진다.
;GLOBAL는 RESETDATA나 LOADDATA에 의해 초기화・덧쓰기되지 않는다.
;필요에 따라서 코멘트 아웃을 해제하는 것.
LOADGLOBAL
RESULT = GLOBAL
SAVEGLOBAL
LOADGLOBAL

[IF_NDEBUG]
    TITLE_HTML = <p align='right'>
    TITLE_HTML += TITLE_PICTURE()
	HTML_PRINT TITLE_HTML
[ENDIF]

;타이틀 표시
$TITLE_SELECT
DRAWLINE 

ALIGNMENT CENTER
PRINTL
PRINTFORML %GAMEBASE_TITLE%
PRINTL
PRINTFORML %eraLAO_VER%
PRINTFORML %GAMEBASE_AUTHOR%
PRINTL
PRINTFORML (%GAMEBASE_YEAR%)
PRINTL
PRINTL
PRINTL
PRINTFORML %GAMEBASE_INFO%
ALIGNMENT LEFT


DRAWLINE 

;선택지 표시._Rename.csv로 지정한 값을 읽을 수 없기에 여기서 지정할 필요가 있다.
PRINTL [0] 처음부터 시작한다
PRINTL [1] 로드해 시작한다

$TITLE_INPUT
INPUT
IF RESULT == 0
	DRAWLINE
	RESETDATA
	;ADDDEFCHARA는 eramaker의 동작을 재현 하기 위해서 존재하는 전용의 함수입니다
	;다른 장면에서는 ADDCHARA, ADDSPCHARA등을 사용하는 것을 권합니다
	ADDDEFCHARA
	BEGIN FIRST
	RETURN
ELSEIF RESULT == 1
	CALL LOADGAME_EX
	GOTO TITLE_SELECT
	;LOADGAME_EX로 LOAD를 실시하지 못하고 돌아왔을 경우, 다시 한 번 더 선택한다.
ELSE
	CLEARLINE 1
	REUSELASTLINE 올바른 값이 아닙니다
	GOTO TITLE_INPUT
ENDIF

;SYSTEM_TITLE중에서 RETURN 하면 다음에 실행하는 행이 없어져, 에러가 된다.
;반드시 BEGIN 명령이나 LOADGAME를 실시하는 것.

@TITLE_PICTURE
#FUNCTIONS
#DIMS TITLE_HTML
#DIMS TITLE_NAME

SELECTCASE RAND:6
    CASE 0
        TITLE_NAME = TITLE1
    CASE 1
        TITLE_NAME = TITLE2
    CASE 2
        TITLE_NAME = TITLE3
    CASE 3
        TITLE_NAME = TITLE4
    CASE 4
        TITLE_NAME = TITLE5
    CASE 5
        TITLE_NAME = TITLE6
ENDSELECT

TITLE_HTML = <img src='%TITLE_NAME%' height='1080' width='1920' ypos='170'>
RETURNF TITLE_HTML
