;현재 집의 상태를 나타내는 함수
@HOME_NAME
#FUNCTIONS

IF FLAG:히든메시지
    LOCALS = ???
ELSEIF FLAG:자택상황 == 0
    LOCALS = 원룸
ELSEIF FLAG:자택상황 == 1
    LOCALS = 주택
ELSEIF FLAG:자택상황 == 2
    LOCALS = 저택
ELSE
    LOCALS = 미확인
ENDIF

RETURNF LOCALS

;SHOP 메뉴(홈화면)의 정보창 함수
@SHOP_STATUS
MONEYSTR MONEY:0
DRAWLINEFORM =
IF FLAG:히든메시지
    PRINTFORML ??? 일째 ?? %HOME_NAME()% 소지금: %RESULTS% 보유 바이오로이드 수 : 1/1
    PRINTFORML 당신의 이름 : %CALLNAME:(FLAG:히든메시지)%의 %CALLNAME:MASTER% 현재 선택 대상 : 당신만의 %CALLNAME:(FLAG:히든메시지)%
ELSE
    PRINTFORML {DAY} 일째 \@ TIME ? 밤 # 낮 \@ %HOME_NAME()% 소지금: %RESULTS% 보유 바이오로이드 수 : {FLAG:현캐릭터수}/{FLAG:캐릭터보유고} 출격 상태 : {FLAG:현출격인수}/{FULL_SORITE}
    PRINTFORML 당신의 이름 : %NAME:MASTER% 현재 선택 대상 : \@ TARGET == MASTER ? 없음 # %CALLNAME:TARGET% \@
ENDIF
DRAWLINEFORM =

;SYSTEM CALL
@SHOW_STATUS
THROW 미실장

@BIO_INFO(ARG)
#DIM R_ID

R_ID = GETCHARA(ARG)
PRINTFORML No.{NO:R_ID} %NAME:R_ID%
PRINTFORMC 소속: %STR:((TALENT:R_ID:13) + 300)%
SIF CSTR:R_ID:캐릭터소개 != ""
    PRINTSL %CSTR:R_ID:캐릭터소개%

@BASIC_BAR
MONEYSTR MONEY:0
IF FLAG:히든메시지
    PRINTFORM 당신의 이름 : %CALLNAME:(FLAG:히든메시지)%의 %NAME:MASTER% 
ELSE
    PRINTFORM 당신의 이름 : %MASTER%
ENDIF
PRINTFORML 소지금: %RESULTS%
PRINTFORML 체력: {BASE:MASTER:체력}/{MAXBASE:MASTER:체력}

@BASE_BAR(ARG)
PRINTFORML 체력: {BASE:ARG:체력}/{MAXBASE:ARG:체력} 기력: {BASE:ARG:기력}/{MAXBASE:ARG:기력}
