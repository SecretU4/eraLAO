;현재 집의 상태를 나타내는 함수
@HOME_NAME
#FUNCTIONS

IF FLAG:히든메시지
    LOCALS = ???
ELSEIF FLAG:자택상황 == 0
    LOCALS = 원룸
ELSEIF FLAG:자택상황 == 1
    LOCALS = 주택
ELSEIF FLAG:자택상황 == 2
    LOCALS = 저택
ELSE
    LOCALS = 미확인
ENDIF

RETURNF LOCALS

;SHOP 메뉴(홈화면)의 정보창 함수
@SHOP_STATUS
MONEYSTR MONEY:0
DRAWLINEFORM =
IF FLAG:히든메시지
    PRINTFORML ??? 일째 ?? %HOME_NAME()% 소지금: %RESULTS% 보유 바이오로이드 수 : 1/1
    PRINTFORML 당신의 이름 : %CALLNAME:(FLAG:히든메시지)%의 %CALLNAME:MASTER% 현재 선택 대상 : 당신만의 %CALLNAME:(FLAG:히든메시지)%
ELSE
    PRINTFORML {DAY} 일째 \@ TIME ? 밤 # 낮 \@ %HOME_NAME()% 소지금: %RESULTS% 보유 바이오로이드 수 : {FLAG:현캐릭터수}/{FLAG:캐릭터보유고} 출격 상태 : {FLAG:현출격인수}/{FULL_SORTIE}
    PRINTFORML 당신의 이름 : %NAME:MASTER% 현재 선택 대상 : \@ TARGET == MASTER ? 없음 # %CALLNAME:TARGET% \@
ENDIF
;체력/기력 기능 현재 없음
;CALL BASE_BAR(MASTER)
;아직 바이오로이드 BASE 설정 안돼서 오류 일으킴
;SIF TARGET
;    CALL BASE_BAR(TARGET)
DRAWLINEFORM =

@BASIC_BAR
MONEYSTR MONEY:0
IF FLAG:히든메시지
    PRINTFORM 당신의 이름 : %CALLNAME:(FLAG:히든메시지)%의 %NAME:MASTER% 
ELSE
    PRINTFORM 당신의 이름 : %CALLNAME:MASTER% 
ENDIF
PRINTFORML 소지금: %RESULTS% 
;CALL BASE_BAR(MASTER)

@BASE_BAR(ARG)
PRINTFORM %CALLNAME:ARG% 
BARSTR BASE:ARG:체력,MAXBASE:ARG:체력,20
PRINTFORM 체력: {BASE:ARG:체력}/{MAXBASE:ARG:체력} %RESULTS% 
BARSTR BASE:ARG:기력,MAXBASE:ARG:기력,20
PRINTFORML 기력: {BASE:ARG:기력}/{MAXBASE:ARG:기력} %RESULTS%

@BASE_TRAIN_BAR

CALL BASIC_BAR

;CALL BASE_BAR(TARGET)
PRINTFORML 현 대상: %CALLNAME:TARGET%

;SYSTEM CALL
@SHOW_STATUS

DRAWLINEFORM =
CALL BASE_TRAIN_BAR
;TIME:2 = datetime
PRINTFORML 시간 경과: {TIME:2}/{TIMELIMIT}
PRINTL
DRAWLINE
;SHOW_USERCOM으로 이동함

