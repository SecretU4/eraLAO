; 머리쓰다듬기
@COM5

IF CFLAG:호감도 > 49
    SOURCE:호감도 += 10
    SOURCE:스트레스 -= 5
    SOURCE:공포 -= 1
ELSE
    SOURCE:호감도 += 3
    SOURCE:스트레스 += 5
ENDIF

@COM5_MSG
PRINTFORML 당신은 %CALLNAME:TARGET%의 머리를 쓰다듬었다.

@COM_ABLE5
RETURN 1

