; 노래시킨다
@COM101

IF CFLAG:호감도 > 49
    SOURCE:호감도 += 10
    SOURCE:스트레스 -= 5
    SOURCE:공포 -= 1
    EXP:가창경험 ++ ; etc3 ++
ELSE 
    SOURCE:소요시간 -= 1
    SOURCE:스트레스 += 1
ENDIF
;슬레이프니르 etc2는 FIRSTTIME()으로 이관됨

RETURN 1

@COM101_MSG
PRINTFORML 당신은 %CALLNAME:TARGET%에게 노래를 시켰다.

@COM_ABLE101
SIF NO:TARGET == 91
    RETURN 1
RETURN 0

