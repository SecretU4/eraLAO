; 무릎베개받는다
@COM8

IF CFLAG:호감도 > 999
    TFLAG:호감도 += 10
    TFLAG:스트레스 -= 5
ELSE
    TFLAG:스트레스 += 10
ENDIF

CALL COM8_MSG

@COM8_MSG
PRINTFORML 당신은 %CALLNAME:TARGET%에게 무릎베개를 부탁했다.

CALL BIO_COM_MSG

@COM_ABLE8
RETURN 1

