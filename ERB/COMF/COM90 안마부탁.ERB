; 안마부탁
@COM90

IF CFLAG:호감도 > 499
    SOURCE:호감도 += 10
    SOURCE:스트레스 -= 5
    SOURCE:공포 -= 1
ELSE
    SOURCE:공포 += 5
    SOURCE:스트레스 += 10
ENDIF

RETURN 1

@COM90_MSG
PRINTFORML 당신은 %CALLNAME:TARGET%에게 안마를 부탁했다.

@COM_ABLE90
RETURN 0

