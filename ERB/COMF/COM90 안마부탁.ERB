; 안마부탁
@COM90

IF CFLAG:호감도 > 499
    TFLAG:호감도 += 10
    TFLAG:스트레스 -= 5
    TFLAG:공포 -= 1
ELSE
    TFLAG:공포 += 5
    TFLAG:스트레스 += 10
ENDIF

CALL COM90_MSG

@COM90_MSG
PRINTFORML 당신은 %CALLNAME:TARGET%에게 안마를 부탁했다.

CALL BIO_COM_MSG

@COM_ABLE90
RETURN 0

