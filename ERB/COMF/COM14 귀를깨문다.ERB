; 귀를깨문다
@COM14

IF CFLAG:호감도 > 799
    TFLAG:호감도 += 10
    TFLAG:스트레스 -= 5
ELSE
    TFLAG:공포 += 5
    TFLAG:스트레스 += 10
ENDIF

CALL COM14_MSG

@COM14_MSG
PRINTFORML 당신은 %CALLNAME:TARGET%의 귀를 살짝 깨물었다.

CALL BIO_COM_MSG

@COM_ABLE14
SIF NO:TARGET == 108
    RETURN 0
RETURN 1

