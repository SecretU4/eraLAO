; 설교듣는다
@COM100

IF !TALENT:짜증 && !TALENT:정신쇠약
    CFLAG:etc1 += 1 ;아자젤 설교 횟수 카운트
    SOURCE:호감도 += 1
    EXP:고유COM경험 ++ ;TODO etc1과 바꾸기
ELSE
    RETURN 0
ENDIF

RETURN 1

@COM100_MSG
PRINTFORML 당신은 %CALLNAME:TARGET%의 설교를 듣기로 했다.

@COM_ABLE100
SIF !TFLAG:연애중여부
    RETURN 0
SIF NO:TARGET != 126
    RETURN 0
RETURN 1
