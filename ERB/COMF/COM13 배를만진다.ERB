; 배를만진다
@COM13

IF CFLAG:호감도 > 499
    SOURCE:호감도 += 10
    SOURCE:스트레스 -= 5
    SOURCE:공포 -= 1
ELSE
    SOURCE:공포 += 5
    SOURCE:스트레스 += 10
ENDIF

EXP:연애COM경험1 ++
SIF TALENT:사랑 ;연모 이상
    EXP:연애COM경험2 ++

RETURN 1

@COM13_MSG
PRINTFORML 당신은 %CALLNAME:TARGET%의 배를 주물럭 거렸다.

@COM_ABLE13
SIF !TFLAG:연애중여부
    RETURN 0
SIF NO:TARGET == 108
    RETURN 0
RETURN 1
