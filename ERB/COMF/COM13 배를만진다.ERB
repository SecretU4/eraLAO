; 배를만진다
@COM13

IF CFLAG:호감도 > 499
    TFLAG:호감도 += 10
    TFLAG:스트레스 -= 5
    TFLAG:공포 -= 1
ELSE
    TFLAG:공포 += 5
    TFLAG:스트레스 += 10
ENDIF

CALL COM13_MSG

@COM13_MSG
PRINTFORML 당신은 %CALLNAME:TARGET%의 배를 주물럭 거렸다.

CALL BIO_COM_MSG

@COM_ABLE13
SIF NO:TARGET == 108
    RETURN 0
RETURN 1

