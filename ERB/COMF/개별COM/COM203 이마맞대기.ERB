; 이마맞대기 ;15번 키스 대응
@COM203

IF CFLAG:호감도 > 799
    SOURCE:호감도 += 15
    SOURCE:스트레스 -= 5
    SOURCE:공포 -= 1
ELSE
    SOURCE:공포 += 10
    SOURCE:스트레스 += 20
ENDIF

EXP:연애COM경험1 ++
SIF TALENT:사랑 && !TALENT:정신쇠약 && !TALENT:짜증 ;연모 이상
    EXP:연애COM경험2 ++

RETURN 1

@COM203_MSG
PRINTFORML 당신은 %CALLNAME:TARGET%의 이마에 당신의 이마를 댔다.

@COM_ABLE203
SIF !TFLAG:연애중여부
    RETURN 0
SIF NO:TARGET != 108
    RETURN 0
RETURN 1
