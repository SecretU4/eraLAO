; 귀에바람 ;14번 귀를깨문다 대응
@COM206

IF CFLAG:호감도 > 799
    SOURCE:호감도 += 10
    SOURCE:스트레스 -= 5
ELSE
    SOURCE:공포 += 5
    SOURCE:스트레스 += 10
ENDIF

EXP:연애COM경험1 ++
SIF TALENT:사랑 && !TALENT:정신쇠약 && !TALENT:짜증 ;연모 이상
    EXP:연애COM경험2 ++

RETURN 1

@COM206_MSG
PRINTFORML 당신은 %CALLNAME:TARGET%의 귀에 바람을 불었다.

@COM_ABLE206
SIF !TFLAG:연애중여부
    RETURN 0
SIF NO:TARGET != 118
    RETURN 0
RETURN 1
