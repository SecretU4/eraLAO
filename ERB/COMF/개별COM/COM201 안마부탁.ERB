; 안마부탁 ;13번 배만지기 대응
@COM201

IF CFLAG:호감도 > 499
    SOURCE:호감도 += 10
    SOURCE:스트레스 -= 5
    SOURCE:공포 -= 1
ELSE
    SOURCE:공포 += 5
    SOURCE:스트레스 += 10
ENDIF

EXP:연애COM경험1 ++
SIF TALENT:사랑 && !IS_CHARA_UNHEALTHY(TARGET, 1) ;연모 이상
    EXP:연애COM경험2 ++

RETURN 1

@COM201_MSG
PRINTFORML 당신은 %CALLNAME:TARGET%에게 안마를 부탁했다.

@COM_ABLE201
SIF !TFLAG:연애중여부
    RETURN 0
SIF NO:TARGET != 108
    RETURN 0
RETURN 1
