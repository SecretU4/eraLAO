; 볼만지기 ;14번 귀깨물기 대응
@COM202

IF CFLAG:호감도 > 799
    SOURCE:호감도 += 10
    SOURCE:스트레스 -= 5
ELSE
    SOURCE:공포 += 5
    SOURCE:스트레스 += 10
ENDIF

EXP:연애COM경험1 ++
SIF TALENT:사랑 && !TALENT:정신쇠약 && !TALENT:짜증 ;연모 이상
    EXP:연애COM경험2 ++

RETURN 1

@COM202_MSG
PRINTFORML 당신은 %CALLNAME:TARGET%의 볼을 살짝 찔렀다.

@COM_ABLE202
SIF !TFLAG:연애중여부
    RETURN 0
SIF NO:TARGET != 108
    RETURN 0
RETURN 1
