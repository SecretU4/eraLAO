;안대당기기 ;15번 키스 대응
@COM207

IF CFLAG:호감도 > 799
    SOURCE:호감도 += 15
    SOURCE:스트레스 -= 5
    SOURCE:공포 -= 1
ELSE
    SOURCE:공포 += 10
    SOURCE:스트레스 += 20
ENDIF

EXP:연애COM경험1 ++
SIF TALENT:사랑 && !IS_CHARA_UNHEALTHY(TARGET, 1) ;연모 이상
    EXP:연애COM경험2 ++

RETURN 1

@COM207_MSG
PRINTFORML 당신은 %CALLNAME:TARGET%의 안대를 잡아당겼다.

@COM_ABLE207
SIF !TFLAG:연애중여부
    RETURN 0
SIF NO:TARGET != 118
    RETURN 0
RETURN 1
