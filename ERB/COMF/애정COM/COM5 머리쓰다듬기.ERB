; 머리쓰다듬기
@COM5

IF CFLAG:호감도 > 49
    SOURCE:호감도 += 10
    SOURCE:스트레스 -= 5
    SOURCE:공포 -= 1
ELSE
    SOURCE:호감도 += 3
    SOURCE:스트레스 += 5
ENDIF

EXP:연애COM경험1 ++
SIF TALENT:사랑 && !IS_CHARA_UNHEALTHY(TARGET, 1) ;연모 이상
    EXP:연애COM경험2 ++

RETURN 1

@COM5_MSG
PRINTFORML 당신은 %CALLNAME:TARGET%의 머리를 쓰다듬었다.

@COM_ABLE5
SIF !TFLAG:연애중여부
    RETURN 0
RETURN 1
