; 등훑기
@COM17

IF TALENT:사랑
    SOURCE:호감도 += 20
    SOURCE:스트레스 -= 5
ELSE
    SOURCE:공포 += 10
    SOURCE:스트레스 += 20
ENDIF

EXP:연애COM경험1 ++
SIF TALENT:사랑 && !IS_CHARA_UNHEALTHY(TARGET, 1) ;연모 이상
    EXP:연애COM경험2 ++

RETURN 1

@COM17_MSG
PRINTFORML 당신은 %CALLNAME:TARGET%의 등을 손가락으로 훑었다.

@COM_ABLE17
SIF !TFLAG:연애중여부
    RETURN 0
RETURN 1
