; 간지럽히기
@COM6

IF CFLAG:호감도 > 49
    SOURCE:호감도 += 10
    SOURCE:스트레스 -= 5
ELSE
    SOURCE:호감도 += 3
    SOURCE:스트레스 += 5
ENDIF

EXP:연애COM경험1 ++
SIF TALENT:사랑 && !TALENT:정신쇠약 && !TALENT:짜증 ;연모 이상
    EXP:연애COM경험2 ++

RETURN 1

@COM6_MSG
PRINTFORML 당신은 %CALLNAME:TARGET%의 옆구리를 찔렀다.

@COM_ABLE6
SIF !TFLAG:연애중여부
    RETURN 0
RETURN 1
