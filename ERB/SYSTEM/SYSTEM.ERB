;========================================================================
;SYSYEM_CALL, BEGIN TURNEND 호출시
;시간대/날짜 변경 처리
;========================================================================
@EVENTTURNEND

SIF CFLAG:MASTER:엔딩플래그
    CALL ENDING(-1)

IF TIME == 0
    TIME = 1
ELSEIF TIME == 1
    TIME = 0
    DAY ++
    CALL DAYEND_SORTIE
    SIF TARGET > 0
        CALL MSG_Morning
ENDIF

;주식 가격은 GLOBAL 변수로 관리하기에 아무 세이브에서 시간이 바뀔때마다 변동
CALL UPDATE_STOCK

BEGIN SHOP

;버전명 출력
@eraLAO_VER()
#FUNCTIONS

LOCALS = {GAMEBASE_VERSION/100}.{(GAMEBASE_VERSION%100)/10}{GAMEBASE_VERSION%10}

RETURNF LOCALS