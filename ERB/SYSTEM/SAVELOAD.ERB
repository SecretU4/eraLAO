;================================================
;파일명: SAVELOAD.ERB
;세이브/로드 작동 관련. 추후 확장 예정.
;
;간단 흐름도
;1. SAVEGAME
;SAVEGAME > INPUT > SAVEINFO > SAVEDATA > RETURN
;=================================================
@SAVEGAME_EX
SAVEGAME

@LOADGAME_EX
LOADGAME

;================================================
;SYSTEM_CALL LOADGAME 이후 실행
;================================================
@EVENTLOAD

CALL VERSION_UP
PRINTL
PRINTW 저장된 세이브를 불러왔습니다.

;================================================
;SYSTEM_CALL SAVEGAME 이후 실행
;로드창에 표시할 세이브파일 정보 설정
;================================================
@SAVEINFO
IF FLAG:히든메시지
    PUTFORM ??? 일째 ? %HOME_NAME()% 이름: %CALLNAME:(FLAG:히든메시지)%의 %CALLNAME:MASTER% 대상 : 당신만의 %CALLNAME:(FLAG:히든메시지)% %eraLAO_VER()%
ELSE
    PUTFORM {DAY} 일째 \@ TIME ? 밤 # 낮 \@ %HOME_NAME()% 이름 : %NAME:MASTER% 대상 : \@ TARGET == MASTER ? 없음 # %CALLNAME:TARGET% \@ %eraLAO_VER()%
ENDIF
